clouds:
  openstack:
    type: openstack
    auth-types: [userpass]
    endpoint: http://10.188.0.7:5000/v3  # keystone/0 unit IP address
    regions:
      RegionOne:
        endpoint: http://10.188.0.7:5000/v3  # keystone/0 unit IP address
credentials:
  openstack:
    default-region: RegionOne
    juju:  # Only the credential name in Juju, not OpenStack username
      auth-type: userpass
      password:  changeme  # OpenStack password
      project-domain-name: user_domain  # Change as appropriate
      user-domain-name: user_domain  # Change as appropriate
      username: juju  # OpenStack username
      version: "3"

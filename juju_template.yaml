clouds:
  openstack:
    type: openstack
    auth-types: [userpass]
    endpoint: http://1.2.3.4:5000/v3  # Keystone IP address
    regions:
      RegionOne:
        endpoint: http://1.2.3.4:5000/v3  # Keystone IP address
credentials:
  openstack:
    juju:  # Only the credential name in Juju, but matching the OpenStack username is sensible
      auth-type: userpass
      password: changeme  # OpenStack password
      project-domain-name: user_domain  # Change as appropriate
      user-domain-name: user_domain  # Change as appropriate
      username: juju  # OpenStack username
      version: "3"
